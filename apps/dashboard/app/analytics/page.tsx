import {prisma} from "@marketplace-engine/database";export const dynamic="force-dynamic";export default async function Analytics(){const territories=await prisma.territory.findMany({include:{_count:{select:{opportunities:true}},opportunities:{include:{tasks:true,lead:true}}}});return <><header><div><p className="eyebrow">PERFORMANCE</p><h1>Analytics</h1><p>Funnel performance by operating territory.</p></div></header><section className="panel"><table><thead><tr><th>Territory</th><th>Opportunities</th><th>Tasks</th><th>Sent</th><th>Replies</th><th>Leads</th></tr></thead><tbody>{territories.map(t=>{const tasks=t.opportunities.flatMap(o=>o.tasks);return <tr key={t.id}><td>{t.name}</td><td>{t._count.opportunities}</td><td>{tasks.length}</td><td>{tasks.filter(x=>x.status==='SENT').length}</td><td>{tasks.filter(x=>Boolean(x.result)).length}</td><td>{t.opportunities.filter(x=>x.lead).length}</td></tr>})}</tbody></table></section></>}

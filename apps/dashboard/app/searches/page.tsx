import {prisma} from "@marketplace-engine/database";
import {updateSearch} from "../admin-actions";
export const dynamic="force-dynamic";
export default async function Searches(){const rows=await prisma.searchDefinition.findMany({include:{source:true,territory:true,cell:true,queryFamily:true,runs:{orderBy:{startedAt:'desc'},take:1}},orderBy:[{priority:'desc'},{name:'asc'}]});return <><header><div><p className="eyebrow">SEARCH PLANNER</p><h1>Search definitions</h1><p>{rows.length} source-specific searches scheduled across canonical cells.</p></div></header><section className="panel"><table><thead><tr><th>Search</th><th>Family</th><th>Cell</th><th>Radius</th><th>Next run</th><th>Status</th><th>Configuration</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.query}</td><td>{r.queryFamily.name}</td><td>{r.cell.cellKey}</td><td>{r.radiusKm} km</td><td>{r.nextRunAt?.toLocaleString('en-CA')??'Due'}</td><td>{r.runs[0]?.status??'Never'}</td><td><form action={updateSearch} className="inlineForm"><input type="hidden" name="id" value={r.id}/><select name="enabled" defaultValue={String(r.enabled)}><option value="true">Enabled</option><option value="false">Paused</option></select><label>minutes <input name="frequencyMinutes" type="number" min="60" defaultValue={r.searchFrequencyMinutes}/></label><label>priority <input name="priority" type="number" min="0" max="100" defaultValue={r.priority}/></label><button>Save</button></form></td></tr>)}</tbody></table></section></>}
